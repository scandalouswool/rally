import React from 'react';
import { expect } from 'chai';
import MenuView from '../containers/MenuView';

describe('MenuView', () => {
  it('should exist', function () {
    expect(MenuView).to.exist;
  });
});
